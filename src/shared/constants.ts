// ============================================================
// Page MCP Plugin — Constants
// ============================================================

/** PostMessage channel used between Content Script and Page */
export const PM_CHANNEL = 'page-mcp';

/** CSS variable prefix for the plugin widget */
export const CSS_PREFIX = 'pmcp';

/** Max conversations to persist */
export const MAX_CONVERSATIONS = 50;

/** Max prompts shown in quick actions */
export const MAX_QUICK_PROMPTS = 3;
